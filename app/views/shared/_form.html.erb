<%= form_with(model: @post) do |form| %>
    <div>
        <%= form.label :title %>
        <br>
        <%= form.text_field :title %>
    </div>
    <div>
        <%= form.label :body, class: "form-control" %>
        <%= form.text_area :body%>

        <%= form.submit %>
    </div>
<% end %>